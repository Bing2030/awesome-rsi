{
  "what": "Minimal, extensible terminal coding agent and harness (pi.dev) whose centerpiece is a self-extensible agent: it writes its own TypeScript extensions, skills, and prompt templates, layered on a stateful agent runtime and a unified multi-provider LLM API.",
  "architecture": [
    "Layered monorepo: `pi-coding-agent` (interactive CLI, bin `pi`) builds on `pi-agent-core` (stateful `Agent` with tool execution and event streaming), which builds on `pi-ai` (unified multi-provider LLM API: OpenAI, Anthropic, Google, …); sibling packages add `pi-tui`, `pi-durable`, `pi-telemetry`, and `chord`.",
    "Self-extension = in-process TypeScript extensions: a default factory receives `ExtensionAPI` and registers tools (`pi.registerTool()`), commands, providers, and event handlers; loaded via `jiti` (no compile step) from `~/.pi/agent/extensions/` or project dirs, or ad hoc with `pi --extension ./hello.ts`. The README's pitch: 'Ask Pi to create the prompt templates, skills, extensions, and themes you need.'",
    "The agent mutates its own tool set at runtime: register every tool, keep optional ones inactive, and a loader tool calls `pi.setActiveTools()`; `examples/extensions/dynamic-tools.ts` registers brand-new tools mid-session. Pi logs the initial prompt/tool set in the transcript's first system message and appends each tool/prompt change before the next model request.",
    "Sessions are JSONL trees — every entry has an ID and parent, branches fork from any entry, compaction inserts summary entries — and all five interfaces (interactive, print, JSON, RPC, TypeScript SDK) share the same agent/session mechanisms. Skills inject instructions on demand."
  ],
  "advantages": [
    "The extension API is ordinary in-process TypeScript the agent itself can author and reload (`ctx.reload()`), so self-extension is a first-class loop — a concrete substrate for studying agents that grow their own tool sets rather than a metaphor.",
    "Each layer is independently reusable: build a custom agent on `pi-agent-core` + `pi-ai` (a dozen lines to stream a tool-calling agent) without adopting the coding agent.",
    "Branch-tree sessions with compaction and forking yield durable, inspectable transcripts; the project actively curates real OSS agent sessions as datasets (badlogicgames/pi-mono on Hugging Face)."
  ],
  "run": "npm install -g --ignore-scripts @earendil-works/pi-coding-agent  # Node >= 22.19; or: curl -fsSL https://pi.dev/install.sh | sh\ncd /path/to/project && pi   # then /login to attach a provider",
  "note": "Read packages/coding-agent/docs/extensions.md and how-pi-works.md, then examples/extensions/dynamic-tools.ts: new tools registered mid-session, active set switched via pi.setActiveTools(). Deliberately ships no built-in permission system — docs/containerization.md covers micro-VM/Docker sandboxing.",
  "_source_note": "Read root README, packages/coding-agent README + package.json (v0.87.1, bin `pi`), docs/extensions.md, docs/how-pi-works.md, docs/quickstart.md, examples/extensions/dynamic-tools.ts, and packages/agent README via raw.githubusercontent/gh api on main. High confidence. Note: the npx form from the prior card is not README-documented (npx appears only for example scripts); the documented install is the npm -g command above."
}

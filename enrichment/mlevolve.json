{
  "what": "MLEvolve — an agentic MLE system that solves Kaggle-style competitions via progressive Monte Carlo Graph Search with multi-agent collaboration; #1 on MLE-bench (65.3% medal rate, 12h) and best on 11 of 15 math-optimization tasks against five specialized systems including AlphaEvolve (arXiv 2606.06473; AutoMLGen successor).",
  "architecture": [
    "Search core — engine/agent_search.py: AgentSearch coordinates the MCGS loop and delegates to engine/node_selection (UCT with _piecewise_decay exploration constant: initial_C=1.414 until T1, linear decay to lower_bound=0.7 by T2 from cfg.agent.decay.phase_ratios; root-locked draft/fusion_draft children), engine/evaluation, engine/execution (Interpreter) and engine/solution_manager; every SearchNode is tagged with a branch_id and tracked in branch_all_nodes/branch_successful_nodes.",
    "Agent suite — agents/: draft/improve/debug plus evolution_agent (intra-branch evolution on stagnation), fusion_agent + aggregation_agent (cross-branch fusion, and multi-branch aggregation when the root hits its draft limit), code_review_agent, data_leakage_agent (metric beyond leakage_max_threshold=0.999 triggers a check); codegen is multi-mode — agents/coder/diff_coder/ SEARCH/REPLACE patching (diff_generate.py, patcher.py, apply.py) vs stepwise_coder.py vs single-pass, dispatched from config flags use_diff_mode/use_stepwise_generation.",
    "Experience memory — agents/memory/global_memory.py: GlobalMemoryLayer persists a MemRecord per node (plan, code summary, stage, success/failure label, parent/current metric, exec_time) into a task-scoped workspace_dir/global_memory dir; agents/memory/retriever.py HybridRetriever fuses BM25Okapi keyword hits with FAISS vector search via Reciprocal Rank Fusion, and agents/planner/planner_with_memory.py uses it for the two-stage retrieval-augmented planning mode.",
    "Stagnation triggers — engine/conditions.py: should_trigger_branch_fusion fires only inside a fusion_min_time_hours–fusion_max_time_hours window (config comment: cross-branch fusion >=6h) when >=fusion_min_branches branches each hold >=fusion_min_successful_nodes successful nodes and is_globally_stagnant is true; is_branch_stagnant counts threshold=3 attempts without beating the branch best; engine/coldstart/ recommends pretrained models per task category from models_guidance_classified.json."
  ],
  "advantages": [
    "The most credible open-source data point in this catalog for long-horizon evolutionary search: #1 on MLE-bench at 65.3% medal rate on a 12-hour budget (baselines mostly get 24h), and per the paper it is best on 11/15 math-optimization tasks against five specialized systems including AlphaEvolve (the README's own tables claim matches-or-beats on 14/15) — evidence the substrate generalizes past Kaggle to algorithm discovery.",
    "Cross-branch fusion is a concrete self-improvement mechanism you can read and ablate: on global stagnation the system merges insights from top-performing nodes across different branches into new candidates and runs trajectory-aware evolution over each branch's full improvement history — richer than the flat restart/population loops most evolutionary coder repos ship.",
    "Memory with honest labels and per-agent retrieval policies: every node's plan/code/metric/success-failure outcome is stored and retrieved via hybrid BM25+FAISS (RRF), with different agents querying memory differently to force novel approaches — plus cold-start guidance per task category, all switchable via use_global_memory/use_evolution/use_fusion/use_aggregation ablation flags."
  ],
  "run": "pip install --no-deps -r requirements_base.txt\npip install --no-deps -r requirements_ml.txt\npip install --no-deps -r requirements_domain.txt\n# edit config/config.yaml: set dataset_dir to the mle-bench data root and agent.code/feedback base_url+api_key (OpenAI-compatible endpoints; if agent.use_global_memory, also set agent.memory_embedding_model_path, and agent.memory_embedding_device: cpu without CUDA)\nbash run_single_task.sh <EXP_ID> <DATASET_DIR> [SERVER_ID]\n# example: bash run_single_task.sh denoising-dirty-documents /mle-bench/data 1\n# results land in ./runs/<timestamp>_<exp_id>/ (search-tree logs, best solution code, top-K submissions)",
  "note": "Start at engine/node_selection.py — _piecewise_decay's exploration schedule plus root-node locking is the whole search policy. Then engine/conditions.py: fusion needs >=N successful branches AND global stagnation inside a time window. Compare fusion_agent (cross-branch) vs evolution_agent (intra-branch); the use_fusion/use_evolution flags exist for exactly that ablation.",
  "_source_note": "Read the full README (timeline, MLE-bench + math-optimization result tables, Key Technical Contributions, Setup, Quick Start, Acknowledgments) and source via gh api: engine/agent_search.py (AgentSearch __init__: branch maps, best_metric_history/stagnation_window, max_fusion_drafts, GlobalMemoryLayer wiring at workspace_dir/global_memory), engine/node_selection.py (_piecewise_decay with initial_C=1.414/lower_bound=0.7, select with root lock), engine/conditions.py (should_trigger_branch_fusion, is_branch_stagnant threshold=3), agents/memory/global_memory.py and retriever.py (GlobalMemoryLayer, HybridRetriever: BM25Okapi + faiss + RRF), run.py, run_single_task.sh (launches local format-validation server on port 5005+SERVER_ID), config/config.yaml (steps=500, time_limit=43200, use_fusion/use_evolution/use_aggregation, leakage thresholds), plus the full file tree. High confidence — class/function names, thresholds and flags quoted verbatim. Correction to the prior card: the README positions MLEvolve as a successor to AutoMLGen (arXiv 2510.08511) that competes against AlphaEvolve, not a descendant of it; it also powers InternAgent 1.5's coding module. Did not execute the code."
}

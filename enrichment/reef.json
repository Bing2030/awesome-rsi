{
  "what": "First open-source infrastructure for continual self-improving agents: an OpenAI/Anthropic-compatible serving layer that records interactions, matches feedback to receipts, trains weights (Slime/SGLang) or evolves harness text (Reefine), and publishes only evaluation-passing updates as versioned releases while serving continues.",
  "architecture": [
    "Serve-Observe-Grow-Commit loop mapped to real modules (README table): reef/service/ takes OpenAI- and Anthropic-compatible requests (/v1/chat/completions, /v1/messages) and records interactions; reef/storage/records.py stores AgentRecords; reef/train/processors/ match reported feedback to eligibility; reef/recipe/ + reef/train/ produce the update; reef/train/evaluation/ + reef/artifact/ gate and publish it.",
    "Feedback is receipt-based: every response carries an x-reef-agent-record-id header; POST /reef/report attaches {score, feedback, references:[receipt]} validated against schemas in reef/core/reports/ (scored_rollout, teacher_context); the x-reef-scenario request header creates or selects the scenario whose recipe decides what evolves — requests never pick recipes.",
    "Backends are pluggable: reef/train/slime_backend/ (Megatron LoRA adapters, Ray executors, weight_updaters) and tinker_backend/ train weights; reef/train/cordis_backend/ proposes harness Mutations; recipes (sao, tttd, gepa, meta_harness, openclawrl, skillclaw) live in the recipes/ cookbook, selected by dotted class reference and not shipped in the wheel.",
    "Versioned delivery: reef/artifact/ keeps an immutable parent-linked release chain (fork, publish with expected_parent, staged commit_release, git+LFS) and rejected candidates never move the serving release; reef/train/evaluation/evaluators.py ships RegressionCheckMixin — select only while the metric stays within margin of the best so far, so serving holds last selected weights — and reef/surface/ delivers accepted artifacts to harness adapters (pi, claude, codex, opencode, terminus)."
  ],
  "advantages": [
    "The only substrate here that learns under live traffic: serving and training share the deployment, updated weights sync in without a restart, and a rejected candidate leaves the current release serving — unlike offline search loops (RRSI) or batch RL frameworks, both of which stop the world to improve.",
    "One receipt/report plumbing over two artifacts: model weights (SAO continual learning; TTT-Discover test-time training with measured TriMul, circle-packing and Erdos minimum-overlap results) or harness text (Reefine writes a change as a skill, rules entry, agent command, or pi extension and test-runs it under bwrap/pasta sandboxing) — weight-level and harness-level RSI in one deployment.",
    "Non-regression is a first-class policy, not a convention: each candidate-evaluation plugin implements evaluate() and decide() in one contract, with best-checkpoint selection running online against the deployment's own traffic."
  ],
  "run": "Python 3.12+, git-lfs required for artifacts. `uv venv && source .venv/bin/activate && uv pip install reef-infra`, then a pure inference server: `uv run reef serve --inference.model-path Qwen/Qwen2.5-1.5B-Instruct`. Harness evolution (no GPUs): `reef serve --recipe reefine --inference.upstream-url http://127.0.0.1:11434 --inference.upstream-model gemma4:26b`, then `reef-pi evolve \"...\"`. Weight training adds `uv pip install -e \".[slime]\" && uv pip install --no-deps --group runtime`.",
  "note": "Trace the core loop from reef/service/app.py through reef/artifact/release_chain.py; read train/evaluation/evaluators.py (RegressionCheckMixin is the online non-regression gate) and compare recipes/sao/ (weights) with reef/recipe/reefine/evolution.py (harness) — the same loop over two artifact types.",
  "_source_note": "Read README.md in full (loop-to-module table, install, weight-training and Reefine deployment walkthroughs, recipe/results table, acknowledgements), pyproject.toml, and source: reef/cli.py (serve/connect commands), reef/artifact/repository.py + release_chain.py (RepositoryBackend fork/publish/commit_release staging), reef/storage/records.py (RecordStore ABC, RecordRetention 7-day/20GiB defaults), reef/train/evaluation/evaluators.py (AlwaysSelectMixin, RegressionCheckMixin with margin-based decide), reef/recipe/reefine/evolution.py (REQUEST_KINDS: skill/rules/agent_command/code_extension), reef/service/app.py, plus the full file tree. Repo metadata: 4,114 stars, Apache-2.0, main pushed 2026-09-23, homepage reefinfra.ai. High confidence."
}
